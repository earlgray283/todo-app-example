# todo アプリ(GraphQL + firestore)

## Setup

**frontend**
```console
$ cd frontend
$ yarn
$ yarn start
```

**backend**

firestore の方を色々やって `credentials.json` と `ProjectID` を取得し、`main.go` を編集

```console
$ cd backend
$ go run .
```
